// Temporary WebSocket server placeholder
// This will be replaced with the full implementation once 'ws' module is installedso

class WebSocketServer {
  constructor(server) {
    console.log('⚠️  WebSocket server is disabled - install "ws" module to enable');
    this.server = server;
  }
}

module.exports = WebSocketServer;
